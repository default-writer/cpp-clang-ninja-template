{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "debug",
      "type": "lldb-dap",
      "request": "launch",
      "preLaunchTask": "build",
      "program": "",
      "cwd": "${workspaceFolder}",
      "windows": {
        "program": "${workspaceFolder}/test-memory.exe"
      },
      "linux": {
        "program": "${workspaceFolder}/test-memory"
      }
    }
  ]
}